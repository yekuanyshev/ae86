variables:
    IMAGE_NAME: supernova0730/ae86

stages:
    - build
    - deploy

build_image:
    stage: build
    image: docker:20.10.20
    services:
        - docker:20.10.20-dind
    variables:
        DOCKER_TLS_CERTDIR: "/certs"
    before_script:
        - docker login -u $REGISTRY_USER -p $REGISTRY_PASS 
    script:
        - docker build --tag $IMAGE_NAME .
        - docker push $IMAGE_NAME

deploy:
    stage: deploy
    before_script:
        - chmod 400 $SSH_KEY
    script:
        - ssh -o StrictHostKeyChecking=no -i $SSH_KEY ubuntu@91.201.215.23 "
            sudo docker rm -f ae86 || true &&
            sudo docker run --name=ae86 -dp 80:8000 \
            -e DB_HOST=172.17.0.1 \
            -e DB_PORT=5432 \
            -e DB_USER=dovakhin \
            -e DB_PASSWORD=yR25t2njlFcYMbWu \
            -e DB_NAME=jyldam \
            -e HTTP_HOST=0.0.0.0 $IMAGE_NAME"
            
            # sudo docker ps -aq | xargs sudo docker stop | xargs sudo docker rm &&  
            # sudo docker run -dp 80:8000 $IMAGE_NAME" 
            # echo $REGISTRY_PASS | docker login -u $REGISTRY_USER --password-stdin &&




    